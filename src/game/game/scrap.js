var selectRandomLocation = (function selectRandomLocation$(matrix = this.matrix, total = countKernel.convolve(matrix), prob = matrix.map((v) => {

    return (v / total);

})) {
    /* select-random-location ../kit/inc/core/function-expressions.sibilant:24:8 */

    console.log("selecting random", matrix, total, prob);
    return reduceUntil(prob, decide(Math.random()), incrementCounter, [0, 0, 0]).slice(1);
});
var selectRandomLocation = (function selectRandomLocation$(matrix = this.matrix, total = countKernel.convolve(matrix), prob = matrix.map((v) => {

    return (v / total);

})) {
    /* select-random-location ../kit/inc/core/function-expressions.sibilant:24:8 */

    console.log("selecting random", matrix, total, prob);
    return reduceUntil(prob, decide(Math.random()), incrementCounter, [0, 0, 0]).slice(1);
});
